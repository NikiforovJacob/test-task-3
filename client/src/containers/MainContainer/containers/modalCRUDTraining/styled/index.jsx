import styled from '@emotion/styled';
import { Button } from 'reactstrap';

const StyledAddTModalBtn = styled(Button)`
  position: fixed;
  bottom: 20px;
  right: 20px;
`;

export default StyledAddTModalBtn;
